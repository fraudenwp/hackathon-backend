x-common-env: &common-env
  BASE_URL: ${BASE_URL}
  DATABASE_URL: ${DATABASE_URL}
  VALKEY_WORKER_URL: ${VALKEY_WORKER_URL}
  SECRET_KEY: ${SECRET_KEY}
  SECRET_KEY_ENCRYPTION: ${SECRET_KEY_ENCRYPTION}
  DEVELOPMENT: "false"
  FAL_API_KEY: ${FAL_API_KEY}
  JWT_SECRET_KEY: ${JWT_SECRET_KEY}
  JWT_ALGORITHM: ${JWT_ALGORITHM}
  JWT_EXPIRATION_MINUTES: ${JWT_EXPIRATION_MINUTES}
  FRONTEND_BASE_URL: ${FRONTEND_BASE_URL}
  R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID}
  R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY}
  R2_ACCOUNT_ID: ${R2_ACCOUNT_ID}
  R2_BUCKET_NAME: ${R2_BUCKET_NAME}
  R2_ENDPOINT_URL: ${R2_ENDPOINT_URL}
  R2_REGION_NAME: ${R2_REGION_NAME}
  LIVEKIT_URL: ${LIVEKIT_URL}
  LIVEKIT_API_KEY: ${LIVEKIT_API_KEY}
  LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET}
  LIVEKIT_WS_URL: ${LIVEKIT_WS_URL}

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      sh -c "alembic upgrade head &&
             uvicorn src:app --workers 4 --host 0.0.0.0 --port 8005"
    ports:
      - "${WEB_PORT:-8005}:8005"
    expose:
      - 8005
    environment:
      <<: *common-env
      COMMAND: web
    deploy:
      resources:
        limits:
          cpus: "4.0"
          memory: 4G
        reservations:
          cpus: "2.0"
          memory: 2G
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import requests; requests.get('http://localhost:8005/health', timeout=5)",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - resai_network
    depends_on:
      valkey-worker:
        condition: service_started
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      sh -c "python -m taskiq worker --workers 4 src.tasks.taskiq_setup:broker --fs-discover --log-level=INFO"
    environment:
      <<: *common-env
      COMMAND: worker
    ports:
      - "6909:6900"
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 4G
        reservations:
          cpus: "1.0"
          memory: 2G
    restart: unless-stopped
    networks:
      - resai_network
    depends_on:
      valkey-worker:
        condition: service_started
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  valkey-worker:
    image: valkey/valkey:8.1.4
    command:
      [
        "valkey-server",
        "--maxmemory",
        "256mb",
        "--maxmemory-policy",
        "allkeys-lru",
      ]
    expose:
      - 6379
    networks:
      - resai_network
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
        reservations:
          cpus: "0.25"
          memory: 128M
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    volumes:
      - valkey_data:/data
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2"

  livekit:
    image: livekit/livekit-server:latest
    command: --config /etc/livekit.yaml
    ports:
      - "7880:7880"
      - "7881:7881"
      - "50000-50100:50000-50100/udp"
    expose:
      - 7880
      - 7881
    volumes:
      - ./livekit-config.yaml:/etc/livekit.yaml:ro
    networks:
      - resai_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
        reservations:
          cpus: "1.0"
          memory: 1G
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:7880/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  valkey_data:
    name: resai_valkey_data

networks:
  resai_network:
    driver: bridge