port: 7880

keys:
  devkey: secret

rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: false  # Set to true for production with external IP

redis:
  address: valkey-worker:6379
  db: 3  # Separate from workers (0) and websockets (5)

logging:
  level: info

room:
  auto_create: true
  empty_timeout: 300  # 5 minutes
  max_participants: 50
